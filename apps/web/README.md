# Web App

Current web/dashboard entrypoint remains `app/main.py` (Streamlit).

Run:

```bash
streamlit run app/main.py
```

This `apps/web` directory is reserved for the modularized frontend migration while preserving current usability.
